export enum DataAccessStatus {
  approved = 'Approved',
  initiated = 'Initiated',
  refused = 'Refused',
}

export enum DataAccessDirection {
  fromPatient = 'FromPatient',
  toPatient = 'ToPatient',
}
